# Lean deps for the Hugging Face Docker image (FastAPI path only — no Streamlit).
# The full requirements.txt (which adds streamlit) is used by Streamlit Community Cloud.
fastapi>=0.110
uvicorn[standard]>=0.29
openai>=1.30
duckdb>=0.10
httpx>=0.27
pydantic>=2.6
python-dotenv>=1.0
